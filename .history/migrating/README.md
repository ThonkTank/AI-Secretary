# Legacy archive

This directory contains pre-refactor code that is kept only for historical reference.

- It is **not part of the active Android source sets** and is **not compiled or tested** by default build tasks.
- It is retained to make it easier to recover prior implementations or compare behavior during future migrations.
- No maintenance or feature work should be done here; all active development belongs under `src/`.
