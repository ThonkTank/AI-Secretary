<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:padding="@dimen/spacing_md"
    android:background="?attr/colorPrimary">

    <ImageView
        android:id="@+id/flameIcon"
        android:layout_width="@dimen/streak_icon_size"
        android:layout_height="@dimen/streak_icon_size"
        android:src="@drawable/ic_flame"
        android:contentDescription="Streak indicator"
        app:tint="@color/streak_starting" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical"
        android:layout_marginStart="@dimen/spacing_md">

        <TextView
            android:id="@+id/streakCurrentText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Current: 0 days"
            android:textColor="@color/colorOnPrimary"
            android:textSize="16sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/streakBestText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Best: 0 days"
            android:textColor="@color/colorOnPrimary"
            android:textSize="12sp"
            android:alpha="0.8" />

    </LinearLayout>

    <com.google.android.material.progressindicator.CircularProgressIndicator
        android:id="@+id/streakProgressIndicator"
        android:layout_width="48dp"
        android:layout_height="48dp"
        app:indicatorSize="48dp"
        app:trackThickness="4dp"
        app:indicatorColor="@color/colorOnPrimary"
        app:trackColor="@color/colorPrimaryVariant" />

</LinearLayout>
