<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/backgroundColor"
    android:padding="16dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <!-- Header -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Backup &amp; Restore"
            android:textSize="24sp"
            android:textStyle="bold"
            android:textColor="@color/colorPrimary"
            android:paddingBottom="16dp" />

        <!-- Info Section -->
        <TextView
            android:id="@+id/backup_info_text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="💾 Backup &amp; Restore\n\nExport: Speichert alle Aufgaben als JSON-Datei\nImport: Importiert Aufgaben zusätzlich zu vorhandenen\nErsetzen: Löscht vorhandene Daten vor Import"
            android:textSize="16sp"
            android:textColor="@color/textPrimary"
            android:padding="16dp"
            android:background="@color/cardBackground"
            android:layout_marginBottom="24dp" />

        <!-- Export Button -->
        <Button
            android:id="@+id/export_button"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="📤 Exportieren"
            android:textColor="@color/textOnPrimary"
            android:background="@color/colorPrimary"
            android:padding="16dp"
            android:layout_marginBottom="12dp"
            android:textSize="16sp" />

        <!-- Import Button -->
        <Button
            android:id="@+id/import_button"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="📥 Importieren (Hinzufügen)"
            android:textColor="@color/textPrimary"
            android:background="@color/backgroundLight"
            android:padding="16dp"
            android:layout_marginBottom="12dp"
            android:textSize="16sp" />

        <!-- Import Replace Button -->
        <Button
            android:id="@+id/import_replace_button"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="⚠️ Importieren (Ersetzen)"
            android:textColor="@color/textOnPrimary"
            android:background="@color/colorAccent"
            android:padding="16dp"
            android:textSize="16sp" />

        <!-- Warning Text -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="⚠️ Wichtig: Backup-Dateien werden im Downloads-Ordner gespeichert."
            android:textSize="14sp"
            android:textColor="@color/textSecondary"
            android:padding="16dp"
            android:layout_marginTop="24dp" />

    </LinearLayout>

</ScrollView>
