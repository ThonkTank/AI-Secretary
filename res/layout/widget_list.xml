<?xml version="1.0" encoding="utf-8"?>
<!-- widget_list.xml - Container für das Home-Screen Widget -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/surface">

    <!-- Header mit Datum-Navigation und Buttons -->
    <LinearLayout
        android:id="@+id/widget_header"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:padding="@dimen/padding_medium"
        android:background="@color/accent">

        <!-- Pfeil links (vorheriger Tag) -->
        <TextView
            android:id="@+id/widget_prev_day"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="◀"
            android:textSize="@dimen/sp_body"
            android:textColor="@color/white"
            android:padding="@dimen/padding_small"
            android:contentDescription="@string/cd_previous_day" />

        <!-- Datum-Label (zentriert) -->
        <TextView
            android:id="@+id/widget_date"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@string/widget_today"
            android:textColor="@color/white"
            android:textSize="@dimen/sp_title"
            android:textStyle="bold"
            android:gravity="center" />

        <!-- Pfeil rechts (naechster Tag) -->
        <TextView
            android:id="@+id/widget_next_day"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="▶"
            android:textSize="@dimen/sp_body"
            android:textColor="@color/white"
            android:padding="@dimen/padding_small"
            android:contentDescription="@string/cd_next_day" />

        <TextView
            android:id="@+id/widget_add"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="+"
            android:textSize="@dimen/sp_heading"
            android:textColor="@color/white"
            android:padding="@dimen/padding_small"
            android:contentDescription="@string/add_item" />

        <TextView
            android:id="@+id/widget_refresh"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="↻"
            android:textSize="@dimen/sp_heading"
            android:textColor="@color/white"
            android:padding="@dimen/padding_small"
            android:contentDescription="@string/refresh_widget" />

    </LinearLayout>

    <!-- Task-Liste -->
    <ListView
        android:id="@+id/widget_list"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:divider="@null"
        android:dividerHeight="0dp" />

    <!-- Empty-State -->
    <TextView
        android:id="@+id/widget_empty"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:text="@string/no_tasks"
        android:textColor="@color/text_muted"
        android:textSize="@dimen/sp_body"
        android:visibility="gone" />

</LinearLayout>
