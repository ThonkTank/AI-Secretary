<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/overlay_dim">

    <!-- Modal-Card -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginStart="20dp"
        android:layout_marginEnd="20dp"
        android:layout_marginTop="32dp"
        android:layout_marginBottom="32dp"
        android:background="@android:color/white"
        android:elevation="8dp"
        android:orientation="vertical"
        android:clickable="true">

        <ScrollView
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:padding="@dimen/padding_large">

                <!-- Header -->
                <TextView
                    android:id="@+id/modal_title"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@string/title_create_recipe"
                    android:textSize="@dimen/sp_heading"
                    android:textColor="@color/text_primary"
                    android:textStyle="bold" />

                <View android:layout_width="match_parent" android:layout_height="16dp" />

                <!-- Titel -->
                <TextView style="@style/FormLabel" android:text="@string/label_titel" />
                <EditText
                    android:id="@+id/input_title"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:hint="@string/hint_recipe_name"
                    android:singleLine="true"
                    android:textSize="@dimen/sp_body" />

                <!-- Mahlzeit-Typ -->
                <TextView style="@style/FormLabel" android:text="@string/label_mahlzeit" android:layout_marginTop="12dp" />
                <Spinner
                    android:id="@+id/spinner_meal_type"
                    android:layout_width="match_parent"
                    android:layout_height="44dp"
                    android:background="@color/surface_elevated"
                    android:padding="@dimen/padding_small"
                    android:contentDescription="@string/cd_spinner_meal_type" />

                <!-- Zeit-Row: Zubereitung + Kochzeit -->
                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="horizontal"
                    android:layout_marginTop="12dp">

                    <LinearLayout
                        android:layout_width="0dp"
                        android:layout_height="wrap_content"
                        android:layout_weight="1"
                        android:orientation="vertical"
                        android:layout_marginEnd="8dp">
                        <TextView style="@style/FormLabel" android:text="@string/label_zubereitungszeit" />
                        <EditText
                            android:id="@+id/input_prep_time"
                            android:layout_width="match_parent"
                            android:layout_height="wrap_content"
                            android:inputType="number"
                            android:hint="15"
                            android:textSize="@dimen/sp_body" />
                    </LinearLayout>

                    <LinearLayout
                        android:layout_width="0dp"
                        android:layout_height="wrap_content"
                        android:layout_weight="1"
                        android:orientation="vertical"
                        android:layout_marginStart="8dp">
                        <TextView style="@style/FormLabel" android:text="@string/label_kochzeit" />
                        <EditText
                            android:id="@+id/input_cook_time"
                            android:layout_width="match_parent"
                            android:layout_height="wrap_content"
                            android:inputType="number"
                            android:hint="30"
                            android:textSize="@dimen/sp_body" />
                    </LinearLayout>

                </LinearLayout>

                <!-- Portionen -->
                <TextView style="@style/FormLabel" android:text="@string/label_portionen" android:layout_marginTop="12dp" />
                <EditText
                    android:id="@+id/input_servings"
                    android:layout_width="80dp"
                    android:layout_height="wrap_content"
                    android:inputType="number"
                    android:hint="2"
                    android:textSize="@dimen/sp_body" />

                <!-- Zutaten-Sektion -->
                <TextView style="@style/FormLabel" android:text="@string/label_zutaten" android:layout_marginTop="16dp" />

                <LinearLayout
                    android:id="@+id/ingredients_container"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="vertical" />

                <Button
                    android:id="@+id/btn_add_ingredient"
                    android:layout_width="wrap_content"
                    android:layout_height="36dp"
                    android:text="@string/btn_add_ingredient"
                    android:textSize="12sp"
                    android:background="@color/surface_elevated"
                    android:layout_marginTop="@dimen/margin_small" />

                <!-- Anleitung (optional) -->
                <TextView style="@style/FormLabel" android:text="@string/label_zubereitung_optional" android:layout_marginTop="16dp" />
                <EditText
                    android:id="@+id/input_instructions"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:hint="@string/hint_instructions"
                    android:minLines="3"
                    android:maxLines="6"
                    android:inputType="textMultiLine"
                    android:gravity="top"
                    android:textSize="@dimen/sp_body" />

                <!-- Tags -->
                <TextView style="@style/FormLabel" android:text="@string/label_tags_optional" android:layout_marginTop="12dp" />
                <EditText
                    android:id="@+id/input_tags"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:hint="@string/hint_tags"
                    android:singleLine="true"
                    android:textSize="@dimen/sp_body" />

                <!-- Buttons -->
                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="horizontal"
                    android:layout_marginTop="24dp"
                    android:gravity="end">

                    <Button
                        android:id="@+id/btn_cancel"
                        android:layout_width="wrap_content"
                        android:layout_height="44dp"
                        android:text="@string/btn_cancel"
                        android:textColor="@color/text_secondary"
                        android:background="@android:color/transparent" />

                    <Button
                        android:id="@+id/btn_save"
                        android:layout_width="wrap_content"
                        android:layout_height="44dp"
                        android:layout_marginStart="16dp"
                        android:text="@string/btn_save"
                        android:textColor="@android:color/white"
                        android:background="@color/accent" />

                </LinearLayout>

            </LinearLayout>

        </ScrollView>

    </LinearLayout>

</FrameLayout>
