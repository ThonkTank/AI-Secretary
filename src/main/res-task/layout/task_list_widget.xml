<?xml version="1.0" encoding="utf-8"?>
<!-- App widget layout: rendered via RemoteViews. Only platform views are supported —
     no RecyclerView, no custom views. ListView is required here (RecyclerView is not
     supported in RemoteViews).
     See: https://developer.android.com/develop/ui/views/appwidgets#remoteviews -->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/bg_forest_gradient"
    android:padding="@dimen/spacing_sm">

    <!-- Category filter + actions header -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingBottom="@dimen/spacing_xs">

        <TextView
            android:id="@+id/WidgetCategoryFilter"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:minHeight="@dimen/touch_target_min"
            android:gravity="center_vertical|start"
            android:padding="@dimen/spacing_sm"
            android:maxLines="1"
            android:ellipsize="end"
            android:text="@string/task_widget_category_all"
            android:contentDescription="@string/task_widget_category_filter_content_description"
            android:textAppearance="@style/TextAppearance.AutoSecretary.Widget.NavLabel" />

        <ImageButton
            android:id="@+id/WidgetSearch"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:padding="6dp"
            android:src="@drawable/ic_search_24"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/task_widget_search_content_description"
            android:scaleType="fitCenter" />

        <ImageButton
            android:id="@+id/WidgetAdd"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:padding="6dp"
            android:src="@drawable/ic_add_24"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/task_widget_add_content_description"
            android:scaleType="fitCenter" />

        <ImageButton
            android:id="@+id/WidgetRefresh"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:padding="6dp"
            android:src="@drawable/ic_refresh_24"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/task_widget_refresh_content_description"
            android:scaleType="fitCenter" />
    </LinearLayout>

    <!-- Task list -->
    <ListView
        android:id="@+id/WidgetTaskList"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:divider="@android:color/transparent"
        android:dividerHeight="@dimen/task_widget_list_divider_height" />

    <!-- Empty state -->
    <TextView
        android:id="@+id/WidgetEmpty"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:gravity="center"
        android:text="@string/task_widget_empty"
        android:textAppearance="@style/TextAppearance.AutoSecretary.Task.Metadata"
        android:visibility="gone" />

</LinearLayout>
