<?xml version="1.0" encoding="utf-8"?>
<!-- Schedule configuration dialog content.
     SchedulingEnabledSwitch toggles daily planning on/off globally.
     ScheduleConfigLoading shows while data loads; ScheduleConfigContainer is revealed
     on success with day rows inflated programmatically by TaskScheduleConfigDialog. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="@dimen/spacing_lg">

    <com.google.android.material.switchmaterial.SwitchMaterial
        android:id="@+id/SchedulingEnabledSwitch"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="@dimen/spacing_md"
        android:text="@string/task_scheduling_enabled_label" />

    <!-- Global buffer settings: pause between task slots, lead time before appointments.
         Persisted in SchedulingSettings on save; applied on the next regeneration. -->
    <com.google.android.material.textfield.TextInputLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="@dimen/spacing_sm"
        android:hint="@string/task_scheduling_pause_label"
        style="@style/Widget.AutoSecretary.Editor.TextFieldLayout">

        <com.google.android.material.textfield.TextInputEditText
            android:id="@+id/SchedulingPauseInput"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:inputType="number"
            android:imeOptions="actionNext"
            style="@style/Widget.AutoSecretary.Editor.TextField" />
    </com.google.android.material.textfield.TextInputLayout>

    <com.google.android.material.textfield.TextInputLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="@dimen/spacing_md"
        android:hint="@string/task_scheduling_lead_label"
        style="@style/Widget.AutoSecretary.Editor.TextFieldLayout">

        <com.google.android.material.textfield.TextInputEditText
            android:id="@+id/SchedulingLeadInput"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:inputType="number"
            android:imeOptions="actionDone"
            style="@style/Widget.AutoSecretary.Editor.TextField" />
    </com.google.android.material.textfield.TextInputLayout>

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <ProgressBar
            android:id="@+id/ScheduleConfigLoading"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center" />

        <ScrollView
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <!-- Day rows inflated programmatically by TaskScheduleConfigDialog -->
            <LinearLayout
                android:id="@+id/ScheduleConfigContainer"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:paddingHorizontal="@dimen/spacing_lg"
                android:visibility="gone" />
        </ScrollView>
    </FrameLayout>

</LinearLayout>
