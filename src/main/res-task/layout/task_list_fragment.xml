<?xml version="1.0" encoding="utf-8"?>
<!-- Task list screen. Managed by TaskListFragment + TaskViewModel.
     Sections from top to bottom:
       1. Day navigation bar (prev/today/next)
       2. Controls card: checklist/manage toggle, search bar
          (search bar visible only in Manage mode, toggled by TaskListFragment)
       3. Task list (RecyclerView via ListRowAdapter) OR empty-state container — only one visible,
          with the "Neue Aufgabe" FAB overlaid bottom-end
-->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:paddingHorizontal="@dimen/spacing_lg"
    android:paddingTop="@dimen/spacing_sm">

    <include
        layout="@layout/include_temporal_nav_bar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="@dimen/spacing_xs" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:background="@drawable/bg_surface_card"
        android:paddingHorizontal="@dimen/spacing_md"
        android:paddingVertical="@dimen/spacing_sm">

        <com.google.android.material.button.MaterialButtonToggleGroup
            android:id="@+id/TaskListToggle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:singleSelection="true"
            app:checkedButton="@id/TaskChecklistButton">

            <!-- Four segments share the row; tightened horizontal padding keeps the
                 longer labels ("Checkliste", "Verwalten") from ellipsizing at 25% width. -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/TaskChecklistButton"
                style="@style/Widget.AutoSecretary.ToggleButton"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:paddingStart="@dimen/spacing_xs"
                android:paddingEnd="@dimen/spacing_xs"
                android:text="@string/task_list_toggle_checklist" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/TaskManagementButton"
                style="@style/Widget.AutoSecretary.ToggleButton"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:paddingStart="@dimen/spacing_xs"
                android:paddingEnd="@dimen/spacing_xs"
                android:text="@string/task_list_toggle_manage" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/TaskUrgencyButton"
                style="@style/Widget.AutoSecretary.ToggleButton"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:paddingStart="@dimen/spacing_xs"
                android:paddingEnd="@dimen/spacing_xs"
                android:text="@string/task_list_toggle_urgency" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/TaskDeadlineButton"
                style="@style/Widget.AutoSecretary.ToggleButton"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:paddingStart="@dimen/spacing_xs"
                android:paddingEnd="@dimen/spacing_xs"
                android:text="@string/task_list_toggle_deadline" />

        </com.google.android.material.button.MaterialButtonToggleGroup>

        <!-- Search bar: visible only in Manage mode, toggled by TaskListFragment -->
        <com.google.android.material.textfield.TextInputLayout
            android:id="@+id/TaskSearchLayout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/spacing_sm"
            android:visibility="gone"
            android:hint="@string/task_list_search_hint"
            app:endIconMode="clear_text"
            style="@style/Widget.AutoSecretary.Editor.TextFieldLayout">

            <com.google.android.material.textfield.TextInputEditText
                android:id="@+id/TaskSearchInput"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:imeOptions="actionDone"
                style="@style/Widget.AutoSecretary.Editor.TextField" />
        </com.google.android.material.textfield.TextInputLayout>

    </LinearLayout>

    <!-- Task list + empty state (only one visible), with the create FAB overlaid bottom-end -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginTop="@dimen/spacing_xs">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/TaskList"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:clipToPadding="false"
            android:paddingBottom="@dimen/task_list_fab_clearance"/>

        <LinearLayout
            android:id="@+id/TaskEmptyState"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="center"
            android:orientation="vertical"
            android:visibility="gone">

            <include
                layout="@layout/include_empty_state"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:visibility="visible" />
        </LinearLayout>

        <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:id="@+id/NewTaskFab"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom|end"
            android:layout_margin="@dimen/spacing_lg"
            android:contentDescription="@string/task_list_new_task"
            android:src="@drawable/ic_add_24"
            app:backgroundTint="@color/color_primary"
            app:tint="@color/color_on_primary" />

    </FrameLayout>

</LinearLayout>
