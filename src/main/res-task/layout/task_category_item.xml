<?xml version="1.0" encoding="utf-8"?>
<!-- Single category row in the category-management dialog. Inflated programmatically by
     TaskCategoryDialog into CategoryManageContainer. Holds an emoji icon field, a name
     field, a hex colour field and a delete button. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:paddingVertical="@dimen/spacing_sm">

    <EditText
        android:id="@+id/CategoryItemIcon"
        android:layout_width="48dp"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:hint="@string/task_category_icon_hint"
        android:importantForAutofill="no"
        android:inputType="text"
        android:maxLength="2" />

    <EditText
        android:id="@+id/CategoryItemName"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:hint="@string/task_category_name_hint"
        android:importantForAutofill="no"
        android:inputType="textCapSentences" />

    <EditText
        android:id="@+id/CategoryItemColor"
        android:layout_width="88dp"
        android:layout_height="wrap_content"
        android:hint="@string/task_category_color_hint"
        android:importantForAutofill="no"
        android:inputType="textNoSuggestions" />

    <ImageButton
        android:id="@+id/CategoryItemDelete"
        style="?attr/borderlessButtonStyle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:contentDescription="@string/task_category_delete_desc"
        android:src="@android:drawable/ic_menu_delete" />

</LinearLayout>
