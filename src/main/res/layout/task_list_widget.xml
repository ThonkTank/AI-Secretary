<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/task_widget_surface_secondary"
    android:padding="8dp">

    <!-- Day navigation header -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingBottom="4dp">

        <TextView
            android:id="@+id/widget_prev_day"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/widget_day_nav_prev"
            android:textSize="16sp"
            android:padding="8dp"
            android:textColor="@color/task_widget_text_primary" />

        <TextView
            android:id="@+id/widget_date_label"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:gravity="center"
            android:textSize="15sp"
            android:textStyle="bold"
            android:textColor="@color/task_widget_text_primary"
            android:text="@string/widget_day_nav_today" />

        <TextView
            android:id="@+id/widget_next_day"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/widget_day_nav_next"
            android:textSize="16sp"
            android:padding="8dp"
            android:textColor="@color/task_widget_text_primary" />
    </LinearLayout>

    <!-- Action bar: refresh -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="end"
        android:paddingBottom="4dp">

        <ImageButton
            android:id="@+id/widget_refresh"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:src="@android:drawable/ic_popup_sync"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/widget_refresh_content_description"
            android:scaleType="fitCenter" />
    </LinearLayout>

    <!-- Task list -->
    <ListView
        android:id="@+id/widget_task_list"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:divider="@android:color/transparent"
        android:dividerHeight="2dp" />

    <!-- Empty state -->
    <TextView
        android:id="@+id/widget_empty"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:gravity="center"
        android:text="@string/widget_empty"
        android:textColor="@color/task_widget_text_muted"
        android:textSize="14sp"
        android:visibility="gone" />

</LinearLayout>
