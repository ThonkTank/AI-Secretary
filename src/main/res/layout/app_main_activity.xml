<?xml version="1.0" encoding="utf-8"?>
<!-- Root shell activity layout. Inflated by MainActivity.
     Structure: FragmentContainerView (content) → BottomNavigationView (bottom).
     BottomNavigationView drives tab selection; MainActivity swaps fragments into Container
     based on the selected tab. No fragment is hardcoded here — navigation is wired in code. -->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:fitsSystemWindows="true"
    android:background="@drawable/bg_forest_gradient">

    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/Container"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/TabBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="@dimen/spacing_md"
        android:layout_marginBottom="@dimen/spacing_md"
        android:paddingVertical="@dimen/spacing_xxs"
        app:menu="@menu/bottom_nav" />
</LinearLayout>
