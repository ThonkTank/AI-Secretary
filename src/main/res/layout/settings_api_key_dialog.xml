<?xml version="1.0" encoding="utf-8"?>
<!-- Content for the "Claude API-Key" settings dialog: the API key (secret, stored encrypted) and an
     optional custom endpoint base URL (for a self-hosted subscription proxy). -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="@dimen/spacing_lg">

    <EditText
        android:id="@+id/SettingsApiKeyInput"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="@string/settings_api_key_hint"
        android:importantForAutofill="no"
        android:inputType="textVisiblePassword" />

    <EditText
        android:id="@+id/SettingsApiKeyEndpoint"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/spacing_md"
        android:hint="@string/settings_api_key_endpoint_hint"
        android:importantForAutofill="no"
        android:inputType="textUri" />

</LinearLayout>
