<?xml version="1.0" encoding="utf-8"?>
<!-- NOTE: This monochrome icon's pathData must stay in sync with ic_launcher_foreground.xml.
     The paths are the same, but split across multiple colored elements in the foreground.
     Update both files together when changing the icon design. -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <!-- Background rectangle -->
    <path
        android:fillColor="@color/icon_monochrome"
        android:pathData="M24,30h60v48h-60z" />
    <!-- Top bar -->
    <path
        android:fillColor="@color/icon_monochrome"
        android:pathData="M24,30h60v12h-60z" />
    <!-- Side handles -->
    <path
        android:fillColor="@color/icon_monochrome"
        android:pathData="M36,22h6v12h-6zM66,22h6v12h-6z" />
    <!-- Checkmark -->
    <path
        android:fillColor="@color/icon_monochrome"
        android:pathData="M39,63l8,8l22,-22l4,4l-26,26l-12,-12z" />
</vector>
