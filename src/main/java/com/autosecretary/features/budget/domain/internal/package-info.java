/**
 * Implementation details of the {@code budget.domain} package.
 *
 * <p>Classes here are internal helpers (recurring pattern detection, template scheduling) and
 * must not be used directly from {@code ui}, {@code application}, or {@code data} layers.
 * Note: Java does not enforce this boundary at compile time — it is a structural convention only.
 */
package com.autosecretary.features.budget.domain.internal;
