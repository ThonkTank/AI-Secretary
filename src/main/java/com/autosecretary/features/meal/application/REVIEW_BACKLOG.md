[consider] LegacyMealImportService.java:359-425 — Private static helpers (`asInt`, `asLong`, `asDouble`, `asBoolean`, `asEnum`, `asDate`, `asDateTime`, `asString`) duplicate the structural intent of `MapperSupport` in `meal.data.internal.mapper`. Key differences are intentional: `asEnum` here is case-insensitive (equalsIgnoreCase loop) vs `Enum.valueOf` in MapperSupport; `asDate`/`asDateTime` support multiple legacy formats + epoch seconds vs ISO-only in MapperSupport. Since the divergence is deliberate and MapperSupport is package-private in a lower layer, sharing it would violate layering. Accept intentional divergence and document why. **Note: correct backlog level is `features/meal/` since MapperSupport lives in data.internal — needs promotion if addressed.**

