[warning] LegacyMealImportService.java:399-425 — Private static helpers (`asInt`, `asLong`, `asDouble`, `asBoolean`, `asEnum`, `asDate`, `asDateTime`, `asString`) duplicate the structural intent of `MapperSupport` in the data layer. They differ slightly (e.g. `asBoolean` handling of "true"/"false" strings, `asEnum` is case-insensitive here). If a parsing bug is found (e.g. locale-sensitive number parsing), it must be fixed in both places. Extract a shared conversion utility accessible to both layers, or accept the intentional divergence by documenting why they must differ.

[nit] TaskMealIntegrationService.java:38 — `recipeScalingService` is directly instantiated (`new RecipeScalingService()`) in the primary constructor, with no secondary injection constructor. This is inconsistent with `CreatePantryItemUseCase` and `ScaleRecipeUseCase`, which both provide a secondary constructor for injection. Add a secondary constructor accepting `RecipeScalingService` to keep the pattern consistent.

