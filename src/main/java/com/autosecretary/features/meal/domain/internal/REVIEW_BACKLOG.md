[consider] WeeklyFoodTargetService.java + HouseholdEnergyService.java — `WeeklyFoodTargetService.calculate()` calls `HouseholdEnergyService.calculateDgeFoodFactor()` — these two static utility classes are coupled. Both are small (36 and 47 lines), stateless, and in the same package. Merging them into a single `HouseholdNutritionService` would reduce files by one and co-locate the caller with its dependency. Tradeoff: the merged class would mix calorie/BMR calculations with DGE food target calculations, which are conceptually distinct. Low urgency.
