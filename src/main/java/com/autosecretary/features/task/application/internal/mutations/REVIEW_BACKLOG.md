[warning] TaskSlotToggleMutation.java:64-78 — `completedPhaseHook` runs inside `database.runInTransaction()`. The hook in CheckOffTaskUseCase bundles DB writes (budget booking, meal task write) with UI side effects (`BudgetWidgetProvider.notifyWidgetUpdate`). The broadcast fires before the transaction commits; if the transaction rolls back the widget was notified for a write that never happened. Fix: move post-transaction side effects (widget notifications) out of the hook, or split the hook into a DB-part (inside transaction) and a notification-part (after commit).

[warning] TaskSlotToggleMutation.java:35-44 — 10-parameter static `execute` method. Parameters are a mix of infrastructure dependencies and per-call inputs with no grouping. Any new cross-cutting concern (e.g. analytics, a second hook) adds another parameter. Fix: convert to a proper class with constructor-injected dependencies (taskDao, completionService, lifecycleManager, transitionDao, callbackDispatcher, database) and a short `execute(taskId, slotId, postWriteAction, completedPhaseHook)` instance method.
