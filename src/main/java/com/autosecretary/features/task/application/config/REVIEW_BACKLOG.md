[warning] TaskScheduleConfigRepository.java:57-63 — `forDay()` calls `dao.readAll()` on every invocation, builds a full 7-entry EnumMap, and discards 6 of 7 entries. The scheduler calls this once per planning day (7 calls per regeneration cycle), causing 7 redundant full DB reads. Fix: add a `readByDayOfWeek(DayOfWeek)` query to `TaskScheduleConfigDAO`, or pre-fetch the full map once and look up by day in the caller.
