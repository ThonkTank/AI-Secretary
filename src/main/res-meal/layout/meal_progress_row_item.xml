<?xml version="1.0" encoding="utf-8"?>
<!-- Compact single-line food group progress row: icon+name | progress bar | percent -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginTop="@dimen/spacing_xs"
    android:gravity="center_vertical"
    android:orientation="horizontal">

    <TextView
        android:id="@+id/MealProgressRowTitle"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="2"
        android:ellipsize="end"
        android:maxLines="1"
        android:textAppearance="@style/TextAppearance.AutoSecretary.Meal.RowSubtitle" />

    <ProgressBar
        android:id="@+id/MealProgressRowBar"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="@dimen/spacing_sm"
        android:layout_weight="3"
        android:max="100"
        android:minHeight="6dp"
        android:progress="0" />

    <TextView
        android:id="@+id/MealProgressRowPercent"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:minWidth="40dp"
        android:textAlignment="textEnd"
        android:textAppearance="@style/TextAppearance.AutoSecretary.Meal.RowSubtitle" />
</LinearLayout>
