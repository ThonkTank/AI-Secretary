package com.autosecretary.application;

/** Distinguishes an invisible event from a visible event whose title is private. */
public enum CalendarVisibility {
    VISIBLE,
    TITLE_HIDDEN,
    HIDDEN
}
