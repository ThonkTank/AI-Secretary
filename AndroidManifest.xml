<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.secretary.helloworld"
    android:versionCode="323"
    android:versionName="0.3.23">

    <uses-sdk
        android:minSdkVersion="28"
        android:targetSdkVersion="35" />

    <!-- Permissions für Update-System -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <application
        android:label="@string/app_name"
        android:theme="@android:style/Theme.Material.Light">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".TaskActivity"
            android:label="Tasks"
            android:exported="false" />

        <!-- ContentProvider für Log-Zugriff durch Claude Code / Termux -->
        <provider
            android:name=".LogProvider"
            android:authorities="com.secretary.helloworld.logs"
            android:exported="true"
            android:grantUriPermissions="true" />
    </application>
</manifest>
